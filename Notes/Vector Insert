Method 1 :
 
vector<int> vec;
vec.insert(pos,val);
ex = vec.insert(vec.begin()+1,4) // Inserts 4 in position 1.

Method 2:

vector<int>vec = {1,2,3,4,5};
vec.insert(pos,size,val);
ex = vec.insert(vec.begin(),5,0);
//Inserts 0 at the beginning 5 times
Resulting vector - {0,0,0,0,0,1,2,3,4,5}

